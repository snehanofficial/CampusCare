# @campuscare/constants

This package contains domain-organized global constants for CampusCare, including roles, permissions, ticket statuses, priorities, asset statuses, and SLA targets.

## Conventions
- Use `const` declarations and `as const` assertions for read-only arrays and objects.
- Group constants in separate files matching their domain (e.g. `roles.ts`, `tickets.ts`).
